{
  "name": "flowtest-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "build": "cd ../frontend && npm install && npm run build",
    "start:production": "npm run build && node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "better-sqlite3": "^9.4.0",
    "axios": "^1.6.7",
    "uuid": "^9.0.1"
  }
}
